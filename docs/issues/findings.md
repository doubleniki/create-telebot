# Current Findings
## Исправлено
- `bin/create-telebot.js`: добавлены `--skip-install`, `--package-manager`, проверка доступности ПМ; подсказки в README учитывают выбранный ПМ.
- `bin/add-scenes.js`: переписан патчинг `src/index.ts` на идемпотентный, совместимый с текущим шаблоном.
- `bin/add-webhook.js`: реальный путь вебхука через `WEBHOOK_PATH`, health-чек, `setWebhook` только при `SET_WEBHOOK=true`, явные ошибки и логирование.
- `src/index.ts`: проверка `BOT_TOKEN`, обработка ошибок `bot.launch`, `bot.catch`.

## Актуальные
- Шаблоны всё ещё копируются вручную из корня, из-за чего добавление новых файлов требует ручного расширения списка.
- Нет тестов или lint/format-пайплайна; изменения CLI и шаблонов без регресс-покрытия.
